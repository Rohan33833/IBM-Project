# COVID-19 Global BI Dashboard - Dependencies
# Tested with Python 3.9 – 3.12 (recommended: Python 3.11)
# Note: Python 3.14 (latest release as of Dec 2025) is not yet fully supported
# by all scientific Python packages on Windows. Use Python 3.11 for best compatibility.

dash>=2.17.1
plotly>=5.22.0
pandas>=3.0.0
numpy>=2.0.0

# Type stubs — required for basedpyright / static type checking
pandas-stubs>=3.0.0
